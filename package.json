{
  "name": "trnybrkt",
  "description": "Test",
  "version": "0.0.1",
  "private": true,
  "license": "Apache-2.0",
  "author": "Rick Tillery",
  "repository": {
    "type": "git",
    "url": "https://github.com/rtillery/trnybrkt.git"
  },
  "engines": {
    "node": ">=10"
  },
  "scripts": {
    "deploy": "gcloud app deploy",
    "start": "node server.js",
    "system-test": "repo-tools test app",
    "test": "npm run system-test",
    "e2e-test": "repo-tools test deploy"
  },
  "devDependencies": {
    "@google-cloud/nodejs-repo-tools": "^3.0.0"
  },
  "cloud-repo-tools": {
    "test": {
      "app": {
        "msg": "Hello, world!"
      }
    },
    "requiresKeyFile": true,
    "requiresProjectId": true
  }
}
